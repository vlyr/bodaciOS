global long_mode_start

section .text
bits 64

long_mode_start:
    ; zero out data segment registers
    xor ax, ax
    mov ss, ax
    mov ds, ax
    mov es, ax
    mov fs, ax
    mov gs, ax

    extern kmain
    call kmain

    hlt
